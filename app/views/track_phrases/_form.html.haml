= form_for @track_phrase do |f|
  - if @track_phrase.errors.any?
    #error_explanation
      %h2= "#{pluralize(@track_phrase.errors.count, "error")} prohibited this track_phrase from being saved:"
      %ul
        - @track_phrase.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :text
    = f.text_area :text
  .actions
    = f.submit 'Save'
